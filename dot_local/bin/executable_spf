#!/usr/bin/env bash
# spf - Superfile wrapper with Zellij & Helix integration

# 1. 启动 Superfile
# 2. 监听文件选择 (需 Superfile 支持或通过 hook)
#    目前 Superfile 尚未像 Yazi 那样有成熟的 chooser 模式，
#    但我们可以尝试通过 zellij action 发送命令到左侧 pane

if ! command -v superfile &>/dev/null; then
    echo "❌ Superfile not installed."
    exit 1
fi

# 简单的启动 wrapper
exec superfile "$@"
