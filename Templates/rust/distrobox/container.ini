[distrobox]
name=project-dev
image=docker.io/library/ubuntu:24.04
pull=true

[home]
# 使用容器内 home，不透传宿主 ~
# 保证可丢弃
shared=false

[mounts]
# 只挂载项目目录
/home/user/project:/workspace

[env]
# 明确工作目录
WORKDIR=/workspace
