# 激活 Devbox 环境
use devbox

#  将 Devbox 里的工具路径暴露给 IDE (VSCode 需要这个来找到 rust-analyzer)
if [ -d ".devbox" ]; then
   export RUST_SRC_PATH="$DEVBOX_PACKAGES_DIR/rustc/lib/rustlib/src/rust/library"
fi

# 如果存在 .env 文件，自动加载其中的变量
# dotenv_if_exists 会自动处理 export
dotenv_if_exists .env