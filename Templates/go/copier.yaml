project_name:
  type: str
  help: 项目名称 (如 my_tool)

module_path:
  type: str
  default: "{{ project_name }}"
  help: Go module 路径 (如 github.com/me/my_tool)

_exclude:
  - "copier.yaml"
  - ".git"

_tasks:
  - just setup
