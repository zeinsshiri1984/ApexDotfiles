set shell := ["bash", "-eu", "-o", "pipefail", "-c"]
set allow-duplicate-recipes := true

import '~/.config/just/justfile_host'
import '~/.config/just/justfile_project'
import '~/.config/just/justfile_user'