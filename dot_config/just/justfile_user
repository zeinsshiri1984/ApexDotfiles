set shell := ["bash", "-eu", "-o", "pipefail", "-c"]

default:
    @just --list

user:
    @just brew
    @just chezmoi

brew:
    @echo "更新 Homebrew 仓库..."
    brew update
    @echo "升级已安装公式..."
    brew upgrade
    @echo "清理旧版本..."
    brew cleanup
