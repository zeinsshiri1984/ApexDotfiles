# --- 搜索行为 ---
# 智能大小写（全小写时不敏感，有大写时敏感）
--smart-case

# 自动忽略 .git 文件夹
--glob=!.git/*

# 搜索隐藏文件（但忽略 .git）
--hidden

# --- 性能优化 ---
# 限制单行最大列数，防止搜索到超长压缩代码行时卡死终端
--max-columns=150
--max-columns-preview

# --- 样式与交互 ---
# 搜索结果配色（类似 grep 的经典高亮）
--colors=line:fg:yellow
--colors=path:fg:magenta
--colors=match:fg:green
--colors=match:style:bold