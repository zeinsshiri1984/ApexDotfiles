# Entry point for all login / non-interactive shells

ENV_FILE="$HOME/.config/env/env.sh"
[ -r "$ENV_FILE" ] && . "$ENV_FILE"

# mise provides runtimes and PATH; bash-only, idempotent
if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate bash)"
fi
