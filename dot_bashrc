# .bashrc

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# --- Interactive Session Only ---

# History control
HISTCONTROL=ignoreboth
shopt -s histappend
HISTSIZE=1000
HISTFILESIZE=2000

# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize

# Make less more friendly for non-text input files, see lesspipe(1)
[ -x /usr/bin/lesspipe ] && eval "$(SHELL=/bin/sh lesspipe)"

# Enable color support of ls and also add handy aliases
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    alias grep='grep --color=auto'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

# Load Starship (if mise didn't load it or just to be safe/override)
if command -v starship >/dev/null 2>&1; then
    eval "$(starship init bash)"
fi

# Auto-launch Nushell logic
# Only if we are in an interactive bash, and we haven't already tried to launch nu (prevent loops if nu crashes)
# And make sure we are not in a sub-shell or specific context if needed.
# For now, simple check: if nu exists, exec it.
if command -v nu >/dev/null 2>&1; then
    # We use 'exec' to replace the bash process with nushell
    # This means 'exit' in nushell will close the terminal window/session, which is desired.
    exec nu
fi
